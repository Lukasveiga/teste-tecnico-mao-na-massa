spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:hotel_system_reservation_db_test
    username: dev
    password: dev
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: create

