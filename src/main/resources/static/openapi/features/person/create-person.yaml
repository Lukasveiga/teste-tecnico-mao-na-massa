post:
  summary: Create a new person
  tags:
    - person

  requestBody:
    $ref: './components/create-person-request-body.yaml'

  responses:
    '201':
      $ref: './components/create-person-response-body.yaml'
    '400':
      $ref: './errors/error-validation-response-body.yaml'