get:
  summary: Get one person
  parameters:
    - in: path
      name: personId
      schema:
        type: integer
      required: true
      description: Numeric ID to inform person id
  tags:
    - person

  responses:
    '200':
      $ref: './components/find-person-response-body.yaml'
    '401':
      $ref: './errors/error-not-found-response-body.yaml'